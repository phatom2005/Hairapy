# Production
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

# Actuator - khong show chi tiet health (DB, disk...) cho client an danh tren prod
management.endpoint.health.show-details=when-authorized
